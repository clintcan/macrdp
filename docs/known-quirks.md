# Known behavioural quirks

- **RDPDR surface is a real NFS mount (Phase 2), read-write, no root/kext/FUSE — `nfsserve` + the built-in `mount_nfs`.** `src/rdpdr/surface.rs`. On connect, **each** redirected filesystem device is exposed by its own **in-process NFSv3 server** (`nfsserve`) bound to `127.0.0.1:0` (ephemeral port); `RdpdrFs` implements `nfsserve::NFSFileSystem` by translating each NFS op into RDPDR over the `RdpdrHandle` — reads via `list_dir`/`read_file`, writes via `write_file` (`DeviceWrite`), `set_len`/`remove`/`rename` (`SetInformation` with FileEndOfFile/Disposition/Rename), and `create`/`mkdir` (`DeviceCreate` with FILE_CREATE/FILE_OPEN_IF) — with a `path↔fileid` cache (NFS addresses by opaque u64 fileid, RDPDR by Windows backslash path — `intern()` assigns a stable id per path and remembers size/kind so `getattr` needn't re-stat; `forget()` evicts on delete/rename). macOS's built-in client mounts it `-o nolocks,vers=3,tcp,rsize=262144,wsize=262144,readahead=4,port=<p>,mountport=<p>,actimeo=5 localhost:/ <mountpoint>` (the modest 256 KiB transfer unit + shallow readahead are deliberate — see the A/V-contention note below). The kernel VFS drives lazy lookups as the user browses, so **full subdirectory navigation works** (this replaces the Phase-1c temp-folder/`NSFilePresenter` mirror, which was top-level-only — subdir contents showed as empty folders). **No root needed** for a loopback NFS mount onto a user-created mountpoint (verified). **`/Volumes` is `root:wheel` (not world-writable) on a stock Mac, so the `/Volumes/<label>` attempt always falls back** to `$TMPDIR/macrdp-rdpdr-<pid>/<label>/` (= `/var/folders/...`, **not** `/tmp` — look there when debugging); the mount still appears in Finder. The `/Volumes` branch only succeeds if macrdp runs as root (e.g. a future privileged-port LaunchDaemon). Cleanup: `Surface::Drop` (connection teardown) aborts the NFS accept-loop task and, on a detached thread, `umount -f`s + removes the mountpoint (+ prunes the empty per-pid parent); **SIGKILL/signal-exit leaks one mount + `$TMPDIR` folder** (Drop skipped — `umount` it manually; the OS clears `$TMPDIR`). **Every** redirected filesystem device gets its own mount/server/`RdpdrFs` (`MacRdpdrHandler` keeps a `HashMap<device_id, Surface>` so a re-announce — or a drive added mid-session — doesn't double-mount; distinct mountpoints come from each device's label, with `prepare_mountpoint` appending a numeric suffix on a label collision). **File reads/writes reuse a kept-open handle** (`RdpdrHandle`'s LRU `HandleCache`, cap `MAX_OPEN_HANDLES`=16, keyed by `(device_id, path, Read|Write)`): `read_file`/`write_file` `acquire()` a cached open handle (opening on a miss) and do NOT close after each op, so a large sequential transfer is ~1 open + N reads/writes instead of N×(open+act+close). Handles close on LRU eviction, on a failed op (`invalidate`), and — crucially — before a `remove`/`rename` (`evict_path` awaits the close so a still-open handle can't defer the delete-on-close or block the rename). Measured: an 8 MiB read = 1 effective open + steady reuse, **except** macOS fires a concurrent readahead burst (~18 parallel RPCs) at file-open that all miss the cold cache at once → a one-time burst of opens, of which all-but-one close their duplicate via the insert-reconciliation (the cache keeps the first winner). That cold-open burst is bounded by readahead depth and amortizes to ~1 open for large files; no single-flight coordination (deemed not worth the async complexity). `set_len`/`remove`/`rename`/`create`/`mkdir`/`list_dir` still open→act→close per call (not cached — they're one-shot). On disconnect the cache is dropped and its open handles leak server-side (the client reaps them when the channel closes). `setattr` only honors a **size change** (truncate/extend via FileEndOfFileInformation); mode/uid/gid/atime/mtime are accepted as no-ops since the surface doesn't model them (the cache's times are a fixed server-start stamp). Verified on FreeRDP end-to-end: read path (`ls` + **subdir nav** + `cat` + byte-exact 200 KB multi-chunk read) **and write path** — create, write, append, truncate-rewrite, `mkdir` + nested write, `rename`, delete file, delete dir, and a **byte-exact 500 KB multi-chunk copy-in (sha256 matches source and read-back)** — plus clean unmount on disconnect; read path also verified on **mstsc**. **Two simultaneous redirected drives** verified on FreeRDP (each mounts as its own volume, serves only its own tree, writes land in the right source, both unmount on disconnect). **`--enable-drive-redirection`-gated.** **Open files with the full `FILE_GENERIC_READ` set, not bare `GENERIC_READ`** (`RdpdrHandle::file_read_access` = `FILE_READ_DATA | FILE_READ_ATTRIBUTES | FILE_READ_EA | READ_CONTROL | SYNCHRONIZE`, plus `FILE_SYNCHRONOUS_IO_NONALERT | FILE_NON_DIRECTORY_FILE` options and broad `FILE_SHARE_READ|WRITE|DELETE`): real Windows honors the requested rights strictly, so a synchronous `IRP_MJ_READ` on a handle opened without `SYNCHRONIZE` is denied with `STATUS_ACCESS_DENIED` — the create succeeds, the read fails. FreeRDP-as-client was lenient and accepted bare `GENERIC_READ`, masking it (same FreeRDP-lenient / mstsc-strict pattern as the handshake-ordering quirk below). Same lesson: **verify RDPDR file ops on mstsc, not just FreeRDP** — the write path (`RdpdrHandle::file_write_access` adds `FILE_WRITE_DATA | FILE_APPEND_DATA | FILE_WRITE_ATTRIBUTES | FILE_WRITE_EA`, and delete/rename open with `DELETE | SYNCHRONIZE`) is verified on FreeRDP and on **mstsc** (create / edit-save / mkdir / rename / delete in a writable folder like `Users\<you>\Documents`). **A `STATUS_ACCESS_DENIED` from writing to a non-writable target — e.g. the `C:\` root or `$Recycle.Bin`, which an unelevated mstsc session can't write — is Windows enforcing its own ACL, not a macrdp bug; it now maps to `NFS3ERR_ACCES` so Finder reports "permission denied" (and `RdpdrHandle` errors carry the `NtStatus` via `RdpdrStatus` for that mapping — `OBJECT_NAME_COLLISION`→EXIST, `NO_SUCH_FILE`→NOENT, etc.). Write somewhere the redirected user actually owns.**
- **Redirected-drive I/O can make audio choppy and video stutter — RDPDR shares the one RDP connection + the server's single socket-writer and single-threaded client loop with EGFX video and RDPSND audio.** Reported 2026-06-18. In `vendor/ironrdp-server/src/server.rs::client_loop`, inbound PDU processing, `dispatch_display` (video), `dispatch_audio` (RDPSND), and `dispatch_events` (CLIPRDR **+ RDPDR**) are cooperative futures in **one `tokio::select!` on one thread**, all sharing **one `SharedWriter` mutex** (`writer.lock().await` per write) and one `Rc<Mutex<self>>`. So a large or bursty RDPDR data PDU monopolizes the writer; audio's lag model (divergence (2)/(3)) then sees the stall and **drops stale waves** (= the choppiness) while video frames back up. Worst for **copy-*to*-drive** (big downstream `DeviceWrite` PDUs, same direction as A/V) and large read bursts (deep readahead = many concurrent reads saturating the shared connection). **Mitigations (two, layered):** (1) **cap the NFS transfer unit + readahead** so RDPDR PDUs/bursts stay small enough to interleave with A/V — `run_mount` uses `rsize=wsize=262144` (256 KiB, was 1 MiB) and `readahead=4` (macOS default ~16 was the source of the ~18-wide read burst). Trades peak drive throughput (more round-trips, kept cheap by the handle cache) for A/V smoothness. (2) **dispatch-partition deprioritizes RDPDR behind EGFX** (vendor server divergence (4)): `dispatch_server_events` now orders each batch CLIPRDR → {EGFX + rest} → RDPDR, so a big `DeviceWrite` can't be written ahead of the video frames in the same batch (the fix specifically for **video stutter** on copy-to-drive — `ServerEvent::Egfx` and `ServerEvent::Rdpdr` share that one task, so without it RDPDR bulk starved EGFX exactly as CLIPRDR used to). Note this partition helps **video** (same task); **audio** rides its own `dispatch_audio` task and only benefits from mitigation (1) + the shared-writer being held for shorter chunks. Verified the smaller-unit mount still reads/writes byte-exact + navigates subdirs on FreeRDP (loopback can't reproduce the bandwidth contention itself — confirm smoothness on a real link). If it's still not smooth enough under heavy drive load, the deeper fix is architectural: replace the N racing dispatch futures with **one writer task that drains all channels audio-first** (can only reorder the single connection's writes, not parallelize them) — a larger vendored change, not done. (Aside: a flaky **CLIPRDR** "no reply in 10 s" → error 1359 teardown shows up with headless `sdl-freerdp`; it's a FreeRDP-test-harness artifact — disable client clipboard (`-clipboard`) when isolating RDPDR — not the same issue.)
- **RDPDR (drive redirection) init: mstsc requires the server to send Server Core Capability Request AND Server Client ID Confirm *before* it replies — FreeRDP is lenient and masks this.** Found 2026-06-16 building RDPDR Phase 1a. The MS-RDPEFS server-init order (3.3.5.1) is: Server Announce → [client: Announce Reply + Client Name] → **Server Core Capability Request → Server Client ID Confirm** → [client: Core Capability Response + Device List Announce]. The first cut sent only the Capability Request on Announce Reply and withheld the Client ID Confirm until *after* the client's Capability Response. **FreeRDP replied to just the Capability Request (lenient) so it worked; mstsc strictly waits for the Client ID Confirm and never sends its Capability Response → the handshake deadlocks after "Client Name" and no drive is ever announced.** The connection otherwise looks healthy (cliprdr/EGFX/audio all init), so the only symptom is "no RDPDR lines." Fix (`vendor/ironrdp-server/src/rdpdr.rs`): on Client Announce Reply, return **both** the Capability Request and the Client ID Confirm; send User Logged On on the Capability Response. Verified on mstsc (C: drive announced) and FreeRDP. Lesson reinforced: **verify RDPDR — and protocol handshakes generally — on mstsc, not just FreeRDP**; FreeRDP's leniency hides ordering bugs a real Windows client enforces. (Debugging note: the server's `start()`/handshake logs are at `debug` under `ironrdp_server::rdpdr`; the agent runs at `info`, and changing a LaunchAgent's `RUST_LOG` needs a full `bootout`+`bootstrap` — `kickstart -k` reuses the loaded job's env and silently keeps the old value.)
- **Password-manager windows and DRM video (Netflix etc.) show up blank/black — macOS excludes protected content from screen capture, and we can't (and shouldn't) override it.** Reported as a "why don't these appear?" question 2026-06-16. macrdp captures the whole display via ScreenCaptureKit and excludes nothing itself (`capture.rs` builds the filter with `with_excluding_windows(&[])`); the blanking is the OS protecting content, which SCK honors — so the protected window simply isn't in the pixels we get. Two distinct mechanisms, same visible result and same root cause ("the OS won't put this in a screen capture"):
  - **App-opted-out windows** — password managers (1Password et al.) set `NSWindow.sharingType = .none` on their vault / secret windows so screen-recording malware can't grab credentials. Such windows come through blank/black or absent. Per-window and display-independent (same on the primary panel or a virtual display).
  - **DRM-protected video** — Netflix / Apple TV+ / Disney+ / Amazon use FairPlay/Widevine hardware DRM; the compositor refuses to composite protected frames into *any* capture, so the video region is black while the page chrome, controls, and cursor render normally. **Audio still plays** (DRM blacks the picture, not the sound) — a useful tell that it's DRM, not a macrdp glitch. Non-DRM streaming (YouTube, Twitch, most HTML5 video) captures fine.

  **Not fixable from macrdp**, by design: ScreenCaptureKit has no "ignore sharing type / capture protected content" option, capturing protected content needs Apple entitlements an unsigned / Developer-ID app can't get, and overriding it would defeat the exact security guarantee these apps rely on (exfiltration over the network is their threat model). It's identical to what you'd see in QuickTime screen recording or a Zoom screen-share. **Do NOT chase a "fix"** — document it as a limitation. User workarounds: copy a credential to the clipboard and paste it across (the CLIPRDR channel syncs text fine); use a browser-extension autofill / menu-bar quick-access panel that may not be capture-excluded; for DRM video there is no workaround.
- **The audio SCStream must bind to the SAME display the video captures, or `--detach-primary` / `--capture-primary` kill audio.** Reported 2026-06-16 (mstsc): with `--virtual-display --detach-primary`, audio cut out on connect (and on reconnect played <1 s then died); with `--virtual-display --capture-primary` it stuttered in/out for a while. Root cause: `audio.rs::capture_loop` bound its `SCContentFilter` to `displays.first()` — the **physical primary** — even when video captured the **virtual** display. Both headless modes act on the physical panel once a client connects (`--detach-primary` disables it via `CGSConfigureDisplayEnabled`; `--capture-primary` `CGDisplayCapture`s + gamma-blacks it), which invalidates an SCStream whose content source is that panel: the audio stream ends (yields `None`), the self-heal restart loop rebuilds it against the same dead source, and you get silence (detach) or in/out thrash as each rebuilt stream briefly delivers then dies (capture). The video stream was immune because it already targets the virtual display, which survives both. Fix: thread the video path's `capture_display_id` into `MacRdpsnd` → `capture_loop` and select the matching display (`displays.iter().find(|d| d.display_id() == id)`, fall back to first), so the audio filter follows the virtual display. The filter is built once and reused across every restart-loop rebuild, so the source stays valid for the whole session. SCK system-audio capture (`captures_audio=true`) records all system output regardless of which display anchors the filter — the display is just the required content source — so binding to the virtual display loses no audio. A `bound_display_id` debug line (`RUST_LOG=macrdp::audio=debug`) shows which display audio attached to. Mirror-primary (no virtual display) was never affected (`target_display_id=None` → `first()`, unchanged).
- **Non-US keyboard layouts (`--keyboard-layout`) translate server-side via UCKeyTranslate and post Unicode — they do NOT switch the Mac's input source.** Built 2026-06-16. RDP clients send PS/2 *positional* scancodes, not characters; `input.rs` maps those to macOS virtual keycodes (also positional) and macOS turns a keycode into a character using **the Mac's active input source** — so a non-US client on a US-configured Mac types the wrong letters. The chosen fix (`src/keyboard_layout.rs`) resolves the requested layout to a macOS `UCKeyboardLayout` (via `TISCreateInputSourceList` on the input-source id, falling back to `TISCopyInputSourceForLanguage`) and, for each ordinary typing key, calls `UCKeyTranslate(keycode, modifier-state)` → Unicode, which `input.rs::key` posts as a string event (keycode 0 + `set_string_from_utf16`, the same mechanism as the RDP Unicode-keyboard path). **The alternative — `TISSelectInputSource` to switch the Mac's active layout — was explicitly rejected**: it would change the *local* Mac's keyboard while connected (visible/disruptive to a local user in mirror mode). Load-bearing details: (1) **Only the contiguous ANSI block is translated** — `is_translatable_keycode` = vk ≤ 0x32 **minus Return (0x24), Tab (0x30), Space (0x31)**; those three plus everything ≥0x33 (arrows, F-keys, keypad, editing) must stay on the keycode path so they behave as *keys*, not literal characters. (2) **Cmd/Ctrl combos are NOT translated** (gated on `!has_cmd() && !has_ctrl()`) so app shortcuts (Cmd+C, Cmd+Q) keep firing as keycodes; Shift/Option(AltGr)/Caps ARE folded into the translation (Option→AltGr chars like €). (3) **Dead keys compose** because `dead_key_state` persists across calls — a dead key returns `Some("")` (swallowed; nothing posted) and the accent lands on the next keystroke. (4) **The key-up is swallowed too** for translated keys — Unicode string events have no release side, so posting a bare key-up would desync the focused app. (5) The Unicode event is posted with **no modifier flags** (the shift/option were already baked into the character) so the app doesn't re-apply them. **Auto-detect is the default**: the KLID arrives in the GCC Client Core Data (`keyboard_layout: u32`, the same block the desktop-size feature reads), so the vendored acceptor captures it (acceptor divergence (2)) onto `AcceptorResult`, the vendored server publishes it to a shared `Arc<AtomicU32>` in `client_accepted` (server divergence (10)), and `input.rs` reads that cell to auto-select the matching layout on the first keystroke (re-checking on change, so a reconnect from a different client is handled) — **US English (0x0409) and unknown (0) keep the positional keycode path**, so the US majority is unaffected. `--keyboard-layout <spec>` overrides auto-detect (force a specific layout); `--keyboard-layout none`/`off` forces positional keycodes (disable translation entirely). Verified: US-layout translation unit-tested end-to-end (A→a, Shift→A, Caps→A, 1→1, Shift+1→!), and auto-detect verified live — sdl-freerdp `/kbd:layout:0x040C` made the server log `client keyboard layout announced klid=1036` and the input handler `auto-selected … layout=com.apple.keylayout.French`. The layout must be installed on the Mac (we pass `include_all=1` so even non-enabled bundled layouts resolve).
- **Ctrl→Cmd Windows-shortcut remap (`--map-ctrl-to-cmd`, opt-in) swaps the modifier per-key in `input.rs`; terminals are auto-suppressed, embedded terminals need `--no-remap-apps`.** Built 2026-06-21 on feedback from Devolutions' (IronRDP's) CTO daily-driving macrdp via mstsc: Windows muscle memory (`Ctrl+C/V/X/…`) doesn't trigger macOS copy/paste (which use Cmd), forcing the client's Win→Cmd key. With the flag on, `input.rs::key` rewrites a **curated vk set** — C V X A Z S F N T W O P R G (copy/paste/cut/select-all/undo/save/find/new/new-tab/close/open/print/reload/find-next) — from `Ctrl+<key>` to `Cmd+<key>` via `post_ctrl_as_cmd`: it clears Control (CGEventFlag + `NX_DEVICE_{L,R}_CTRL`) and sets Command (`NX_DEVICE_L_CMD`) in the swapped flags, posts a `FlagsChanged` carrying those on **both** event sources (so `[NSEvent modifierFlags]` reads Cmd), then the key-down; the matching key-up (tracked in a `remapped_keys: HashSet<u16>`, mirroring `consumed_keys`) posts with swapped flags then restores the real Ctrl state via `post_flags_changed`. Shift/Caps carry through, so `Ctrl+Shift+Z` → `Cmd+Shift+Z` (redo) works. **Load-bearing choices:** (1) **Off by default** — the US/default path is untouched; only opt-in. (2) **Q is deliberately excluded** (Cmd+Q quits — a nasty surprise from Ctrl+Q), and **nav keys are untouched** (Mac word-nav is Option+arrow, not Cmd+arrow). Windows redo `Ctrl+Y` is NOT mapped — Mac redo is `Cmd+Shift+Z`, reachable via the Z mapping. (3) **Gate** = `MAP_CTRL_TO_CMD && has_ctrl() && !has_cmd() && !has_alt() && is_remappable_shortcut(vk) && !frontmost_is_excluded()` — so `Ctrl+Alt`/`Ctrl+Cmd` combos and non-set keys pass through unchanged. (4) **Terminal suppression** keeps `Ctrl+C` = SIGINT: `frontmost_is_excluded()` matches the frontmost app's bundle id against a **built-in standalone-terminal list** (Terminal, iTerm2, Alacritty, kitty, WezTerm, Ghostty, Hyper, Warp) plus the user's `--no-remap-apps`. (5) **Embedded terminals (VSCode's integrated TTY etc.) can't be auto-detected** — the frontmost app is the IDE, not a terminal — so they're covered by `--no-remap-apps com.microsoft.VSCode,…` (config `NO_REMAP_APPS`). Trade-off in a listed app: Ctrl stays Ctrl everywhere (integrated-terminal `Ctrl+C` = SIGINT works; editor copy uses Cmd+C, the app's native macOS copy). Helper-process ids prefix-match (`com.microsoft.VSCode` covers `com.microsoft.VSCode.helper.*`). The branch sits **before** the layout-translation branch in `key()` so a remapped key-up isn't swallowed by the layout path's swallow-key-up rule. (Pairs with the `--enable-h264` README/startup nudge from the same CTO feedback — the legacy bitmap path logs "video codec: legacy bitmap path … pass --enable-h264" at startup so "not sure what it negotiated / it's grainy" has an answer.)

  **Resolving "what app is focused right now" in a headless session was the hard part (and needed FOUR signals, last-wins, in `LAST_FOCUS_BUNDLE`).** No single macOS API covers every case under `--virtual-display`/`--capture-primary`: (a) `AXFocusedApplication` polled from a background thread (the existing MRU focus poller) works for normal apps but **returns nothing for an Electron app focused by mouse-click**, and the per-keystroke direct AX call returns `None` on the tokio **input thread** entirely; (b) an event-driven **`NSWorkspace` activation observer** (`init_focus_observer`, registered on the `runloop_thread` because NSWorkspace notifications need a pumped CFRunLoop; the activated `NSRunningApplication` comes straight off the notification `userInfo`, so it's the **main** app id) catches normal click-activation and real app launches — but it does NOT reliably fire for our own AX-driven Cmd+Tab activation (see the fourth feeder below), and an Electron app like **VSCode takes *key* focus without ever *activating*** (its keystrokes land in it, yet it emits no activation and no AX focus change), so the observer never sees it either; (c) the fix that finally caught it is a **click hit-test** (`update_focus_from_click`): on every mouse-down (when `--map-ctrl-to-cmd` is on) we `AXUIElementCopyElementAtPosition` the window under the cursor → owner pid → bundle id, run on the `runloop_thread` (AX is reliable there, not on the input thread), using `self.last_x/last_y` which are already global top-left-origin screen coords. These write `LAST_FOCUS_BUNDLE` last-wins; `frontmost_is_excluded` reads it first (then the direct AX call, then MRU front). (d) A **fourth feeder** (fix `641d887`): our own **Cmd+Tab/Option+Tab** AX activation doesn't reliably post an NSWorkspace activation, so the observer (b) missed it — cycling from an excluded app (terminal/VSCode) to a normal one (e.g. Firefox) left the remap suppressed (Ctrl+T did nothing) until a mouse click refreshed the cell. `commit_cycle_session` now writes the landed app's bundle to `LAST_FOCUS_BUNDLE` directly. Helper pids resolve to the owning app via `bundle_identifier_for_pid`'s fallback (`proc_pidpath` → outermost `.app` → `NSBundle.bundleIdentifier`), so a focused VSCode renderer-helper still reads as `com.microsoft.VSCode`. **Display caveat surfaced during this work:** `--capture-primary` already puts the virtual display at origin (it IS primary), but a window opened *before* connecting stays on the now-captured physical panel and is invisible/unclickable over RDP — `osascript … set position of window 1 to {100,100}` (or just Cmd+Tab to it / open a new window) brings it onto the visible virtual display, after which click-focus detection works on it like any other app. Verified end-to-end on mstsc: Terminal/TextEdit/Safari/VSCode (incl. integrated terminal SIGINT vs editor Cmd+C), via both Cmd+Tab and click.
- **Client-resolution auto-adopt (default; `--no-client-resolution` to opt out) MUST live in the acceptor — the Confirm Active capset cannot reveal the client's requested size.** Built 2026-06-12 to kill mstsc's client-side rescale (typing latency + H.264 audio drift when the served resolution ≠ the client display's). The client's own requested resolution exists in exactly one place: the GCC **Client Core Data** `desktop_width/height` of the MCS Connect Initial. The tempting alternative — upstream's `RdpServerDisplay::request_initial_size`, fed from the client's Confirm Active bitmap capset — is a dead end for detection: conformant clients **overwrite their desktop size with the server's Demand Active values and echo those back** (verified in FreeRDP's `rdp_apply_bitmap_capability_set` source AND live: sdl-freerdp `/size:1024x768` echoed our 1512×982; mstsc follows the same `desktopResizeFlag` contract). Worse, upstream `ironrdp-acceptor` parses the Client Core Data and discards everything but the early-capability flags, and Demand Active is sent *inside* `accept_finalize` — before any server-side code can react. So: `vendor/ironrdp-acceptor` (new fork, divergence log inside) gains `honor_client_desktop_size` — in `BasicSettingsWaitInitial` it adopts the client's core-data size (sanity band 200..=8192 per MS-RDPBCGR) into `desktop_size` + the server Bitmap capset BEFORE Demand Active, so the session is negotiated at the client's resolution from the start with **no deactivation-reactivation resize**. (A reactivation-based variant — emit `DisplayUpdate::Resize` as the first update — was built first and discarded: it cost an extra full reactivation per connect and still couldn't *detect* anything for echo clients.) The vendor server forwards the flag (`RdpServer::set_honor_client_desktop_size`, divergence 9); `CaptureDisplay::request_initial_size` then adopts the echoed size into the process-wide `SharedDesktopSize` (capture.rs) that capture, `input.rs` mouse scaling, and the `h264.rs` surface/encoder all read — input reads per-event, h264 pins dims per connection in `setup_locked`. Costs are exactly the documented non-native-capture trade-offs below (full-frame legacy updates; on aspect mismatch the picture is **letterboxed/pillarboxed by default** to preserve the Mac's aspect ratio, `--stretch` opts into fill-and-distort — see the scaling note); that's the accepted deal — the prior workaround was passing `--width 1920 --height 1080` manually. Verified live with sdl-freerdp at 1024×768 (legacy) and 1280×800 + 800×600 (H.264/EGFX, fresh surface per connection, acks flowing); mstsc verification pending a real Windows client. Disabled automatically when `--width`/`--height`/`--hidpi`/`--virtual-display` pin a size.
- **Server-side scaling forces full-frame updates; aspect handling is letterbox-by-default on the auto-size path, stretch otherwise.** Capturing at a configured size ≠ the Mac's native size makes SCK scale internally, and SCK's dirty-rects then come in *source* (native) coords (clients that strictly respect tile coordinates render a black canvas with only the cursor sprite — mstsc/RemoteFx is the worst offender). `capture.rs` works around that by detecting the mismatch up front and skipping dirty-rect emission (full BitmapUpdate every tick — the upstream encoder's framebuffer diff still keeps unchanged-region cost low). For the aspect dimension there are two modes, both with `scales_to_fit=true`:
  - **Letterbox/pillarbox (default on the auto-size path, i.e. client-resolution auto-adopt with no `--stretch`):** `preserves_aspect_ratio(true)` — SCK fits the Mac into a **centered** sub-rect of the output and pads the rest with black bars, so the picture isn't distorted. `input.rs::move_to` then maps client coords into that centered content rect (clicks in the bars clamp to the nearest edge) via the pure, unit-tested `map_client_to_display`; the shared `SharedDesktopSize.letterbox` flag tells it which mode is live. **Verified 2026-06-15 on macOS 26** by dumping a captured frame: a 1512×982 Mac → 1920×1080 client produced a centered 1663×1080 image with **exactly 128 px bars each side** (= `(1920−1663)/2`), matching the input math — so clicks align. (No inverse-`preservesAspectRatio` surprise on 26; if a future macOS flips it, the bars will land on the wrong axis and clicks will skew — re-dump a frame to check.)
  - **Stretch/fill (`--stretch`, or any explicit `--width`/`--height`):** `preserves_aspect_ratio(false)` — source stretches to fill the whole output (no bars), with the old caveat of non-uniform scaling on aspect mismatch (e.g. 16:10→16:9 ≈ 13.5% vertical compression) and `input.rs` mapping linearly across the full frame.

  Cost at non-native sizes either way: higher bandwidth (full frames) and, on **mstsc with `--enable-h264`**, the scaling amplifies its trailing-frame presentation lag (see the flush-burst note) — a Mac whose native resolution already matches the client serves 1:1 with no scaling and is markedly snappier (confirmed live: a 1512×982 MacBook Pro lags into a 1080p mstsc session while an M1 mini natively driving 1920×1080 does not). Prefer native if you can.
- **No server PointerPosition forwarding (intentional).** mstsc and Microsoft Remote Desktop local-predict the cursor from the user's mouse input; any `PointerPositionAttribute` PDU we send arrives one encode-plus-network round-trip late and snaps the cursor back to a stale position on fast moves. The current design (`poll_shape` only, `poll_position` defined but unwired) is deliberately the right one for all interactive use. *Do not* simply wire `poll_position` up — that's the bug, not the fix. The only thing this design misses is Mac-side *programmatic* cursor moves (an app calling `CGWarpMouseCursorPosition`, rare); fixing that properly requires emitting position only when the Mac cursor diverges from where the last RDP mouse event left it and enough time has passed since that event, which needs shared state between `input.rs` and `cursor.rs`.
- **Legacy codec mismatch with the macOS client (only without `--enable-h264`).** In the legacy bitmap path, Microsoft Remote Desktop / Windows App on macOS offers only NSCodec while the server advertises RemoteFx/QOI, so they fall back to raw/RLE BitmapUpdate — works, but bandwidth-heavy. With `--enable-h264` that same client negotiates AVC420 over EGFX and renders H.264 (verified), which is far better — prefer it for the macOS client.
- **EGFX capability parsing must tolerate unknown versions (Windows App fix).** Windows App for Mac (`com.microsoft.rdc.macos`) advertises an EGFX capset version that older ironrdp_egfx didn't recognize; the `CapabilityVersion::try_from` returned `Err` on it, which the CapabilitiesAdvertise decode propagated as a *fatal* error — killing the whole connection during negotiation, before the AVC-vs-legacy fallback could run. So `--enable-h264` made Windows App unable to connect at all (it connects fine without). Fixed in upstream ironrdp_egfx via PR #1298 (merged 2026-05-25); we pin the ironrdp rev at-or-after `67f3c63` and the previously-vendored `vendor/ironrdp-egfx/` was dropped.
- **A no-AVC EGFX client must be declined at the CHANNEL level — confirming caps and then sending legacy updates is a protocol violation Windows App for Android hard-disconnects on.** Found 2026-07-04, first live Android test. Windows App for **Android** advertises the EGFX pipeline with **`AVC_DISABLED` on every capset** (unlike the macOS Windows App and mstsc, which advertise AVC420). macrdp only implements AVC420 over EGFX, so the old fallback confirmed the client's caps (upstream `GraphicsPipelineServer` queues the `CapabilitiesConfirm` unconditionally — there is no decline path) and then simply never used the channel, serving legacy BitmapUpdate — and the Android client treated "graphics pipeline confirmed but graphics arriving as legacy updates" as a protocol error and dropped the connection ~2 s after activation (`peer closed connection without sending TLS close_notify`; the user sees "protocol error"). Fix (vendored `ironrdp-server` divergence (14) + `h264.rs`): `GfxDvcBridge` gains a shared per-connection **decline flag**; when `on_ready` sees `client_supports_avc == false` it sets the flag and the bridge **discards the connection's EGFX output — the CapabilitiesConfirm is never sent** — leaving the client in the normal "pipeline not yet active" state, in which every client renders the legacy updates we actually send (there is no decline PDU in MS-RDPEGFX; the alternatives are never-confirm or a DVC close, and never-confirm is the minimal one that matches the pre-activation state clients already tolerate). **Verified with sdl-freerdp both ways** (`/gfx:progressive` advertises the exact same AVC_DISABLED signature as Android → declined, stays connected on legacy, zero protocol errors; plain sdl-freerdp (AVC420) → H.264 fully active, unaffected) **and LIVE on the real Windows App for Android 2026-07-04: connects and stays connected** (previously hard-disconnected ~2 s in). Log marker: `EGFX client advertised no AVC420 support — declining the graphics pipeline (no CapabilitiesConfirm) and serving legacy BitmapUpdate`.
- **H.264 wire format must be Annex-B, and mstsc retains EGFX surfaces.** Two hard-won facts behind `src/h264.rs` (`--enable-h264`): (1) the AVC420 payload must be **Annex-B** (start codes + in-band SPS/PPS) — Microsoft's decoder gives zero frame-acks for length-prefixed AVCC, so the default is Annex-B (`MACRDP_H264_LENGTH_PREFIXED=1` flips it for ironrdp-decoder interop). (2) **mstsc retains EGFX surfaces by id for its whole process lifetime** (across reconnects and even macrdp restarts) and no-ops a `CreateSurface` for an id it already holds → frames decode into a stale surface → black screen + live cursor on reconnect. We use upstream's auto-allocated surface id and **document the limitation** rather than fight it: the reliable recovery is to fully close and reopen the mstsc window, which clears its surface cache. **Do NOT try to fix this with `DeleteSurface`** — deleting a surface mstsc doesn't currently hold (or one mapped to the output) breaks its GFX channel entirely (no acks, then disconnect); confirmed twice. **A fresh-unique-id-per-session workaround was tried and removed** (it lived in a vendored `create_surface_with_id`): on mstsc it only *unreliably* avoided the blank — sometimes the desktop drew, sometimes not — and since the reliable recovery (close+reopen mstsc) is identical with or without it, it wasn't worth a permanent upstream divergence. FreeRDP (with H.264) has none of these quirks — it renders and reconnects cleanly, which is how we proved the server output is spec-correct. The residual mstsc reconnect blank is therefore a documented client limitation, not a server bug. **Re-audited 2026-06-22 (the "did we miss a cache-clear PDU?" question): no.** MS-RDPEGFX has no "reset all surfaces / clear surface cache" PDU; `RESET_GRAPHICS` (sent via `resize_with_monitors`) resets only the desktop size + monitor layout, NOT surfaces; the bitmap-cache PDUs (`CacheImportOffer`/`EvictCacheEntry`/`SurfaceToCache`/`CacheToSurface`) are for offscreen bitmaps, not the AVC surface (we reject the client's `CacheImportOffer` with an empty reply). Confirmed against the pinned upstream `GraphicsPipelineServer` API: the only surface-clearing primitive exposed is per-id `delete_surface`; `next_surface_id` (which resets to 0 on each fresh per-connection server — the root of the id-0 collision) has no public setter; and `create_surface_with_id` no longer exists upstream (egfx is no longer vendored) — so monotonic ids would require re-vendoring egfx again. The one not-cleanly-tried server-side idea is a climbing (persisted) surface id with **map-new-then-delete-old, guarded to only delete a prev id confirmed created+mapped+acked** (the earlier climbing-id tries failed either by accumulating un-deleted surfaces → mis-composite, or by deleting a still-mapped/never-created id → channel break); it stays parked because it's mstsc-fragile and needs re-vendoring. **What's still missing is disconnect-side evidence** — whether the EGFX channel-close fires on a *graceful* mstsc disconnect vs an *abrupt* window-close, which decides whether a "delete before teardown" approach is even reachable. Debug instrumentation for that exists (logging-only, no behavior change): run with `RUST_LOG=macrdp::h264=debug,ironrdp_egfx=debug` and watch the `EGFX on_close` / `building fresh GraphicsPipelineServer` / `on_surface_created` / `on_cache_import_offer` markers. **Captured on real mstsc 2026-06-22 — result: `EGFX on_close` NEVER fires, and it's structural.** The vendored `ironrdp-server` teardown (`client_loop` → `RunState::Disconnect` → `on_disconnected`) does not call `close()` on the DVC channels, so the EGFX `on_close()` is never invoked on any disconnect; macrdp only learns the old connection died indirectly (its ship loop sees the output channel drop) *after* the next connection has already begun. **So there is no disconnect-time hook at all — the "DeleteSurface before teardown" idea is dead** (no callback to send from, and the socket is gone by then). The only remaining lever is a *connect-time* `DeleteSurface(0)` on reconnect, which would require re-vendoring egfx again, can't reliably distinguish "this mstsc still holds surface 0" from a fresh client, and broke mstsc's GFX channel in prior variants — so it stays unpursued. **xrdp reference + the full investigation (2026-06-22→23) — the blank is TWO stacked mstsc bugs:** xrdp handles this with the same fixed surface id 0 but, on cleanup, `xrdp_egfx_shutdown_full` sends `DELETE_SURFACE(0)` then a clean **DVC-channel close** before teardown. An early delete-only attempt (gated on `Ok(RunState::Disconnect)`) seemed to fail with a `Broken pipe`, but that was the **60fps H.264 frame-write losing the `select!` race** that run, NOT mstsc being inherently abrupt (an earlier version of this note wrongly concluded "mstsc never gives a graceful-disconnect window" — that's corrected here). **Corrected:** mstsc's connection-bar **X** (close, keep process) is a GRACEFUL disconnect that arrives as an io-channel **Shutdown Request** ("Got disconnect request"); the client loop exits `Ok(RunState::Disconnect)` with the **socket still alive**, so the disconnect hook IS reachable. We then sent `DELETE_SURFACE(all)` **+ a `DYNVC_CLOSE` of the Graphics channel** there — faithfully mirroring xrdp's full `shutdown_full`. Result on real mstsc: the symptom **changed but didn't resolve** — delete-only → still black; delete **+ DYNVC_CLOSE → *transparent*** (the client's own local desktop shows through the mstsc window, remote non-interactive). So the blank is **TWO bugs:** **(1) black** = mstsc retains surface 0 across the drop and composites the stale surface — the channel-close FIXES this (black→transparent); **(2) transparent** = mstsc won't re-map/composite a *fresh* output surface on an in-process reconnect — **not fixable from the server**. xrdp gets past layer (2) via its reconnect-side architecture we can't replicate (per-connection frontend over a persistent X session + a full-screen invalidate/repaint). **DECISION: STOP — documented mstsc client limitation.** Mirroring xrdp's entire shutdown sequence only clears layer (1); the reliable recovery stays close+reopen mstsc (a *fresh* process has neither layer's state — verified to render). FreeRDP/Thincast are unaffected. (Experiment branch `egfx-disconnect-diag`, commit `5453612`, was pushed then deleted.) **Do NOT re-attempt a server-side fix unless someone first cracks mstsc's reconnect re-composite — layer 2.** See the `project_h264_reconnect_blank` memory for the full investigation. **UPDATE 2026-07-02 — the blank is now server-DETECTABLE, and an automatic recovery ships (experimental, default ON, `MACRDP_BLANK_RECOVERY=0` disables).** A decrypted-pcap comparison (SSLKEYLOGFILE + tshark, `-d tls.port==3390,tpkt` is the load-bearing decode-as; note tshark's EGFX dissector fails "payload too big" on WireToSurface1 PDUs >~128 KiB, so server-direction dissection lies after big IDRs — trust the client direction) of a blank vs. rendering reconnect proved: the server sequence is byte-identical, the client decodes + FrameAcks + QoE-acks *everything* while blank, but its **QoE Frame Acknowledge `timeDiffEDR` (decode+render time) is 0 on every single frame** — a rendering session shows nonzero EDR on its first QoE callbacks (~100–230 ms after connect; observed 7–14 ms for the initial full-screen present, so the disarm is effectively instant). The extracted H.264 elementary stream from the blank session decodes to a perfect live desktop, closing the "maybe we sent black" conjecture for good. `h264.rs` feeds that field from `on_qoe_metrics` into a detector (`should_blank_recover`: ≥40 all-zero-EDR QoE callbacks — upstream delivers ~8/s, ~1 per DVC batch, NOT per wire PDU — + regular acks flowing + ≥3 s connected) and, on detection, escalates through two levers (`blank_action`): **attempt 1 = non-destructive REMAP** (`perform_blank_remap`: CreateSurface with a fresh id + MapSurfaceToOutput over the same origin + forced IDR — no delete, no reset; the stale surface is deliberately leaked), **attempt 2 = DROP the connection** (`perform_blank_drop`: `ServerEvent::Quit` → per-connection disconnect; mstsc auto-reconnects via its reconnect cookie and a fresh connection usually renders, re-checked by the fresh detector). **The resize-dance variant was tried first and is a confirmed DEAD END:** upstream `resize_with_monitors` sends DeleteSurface-of-the-mapped-surface as its first PDU, and on a live blank session that killed mstsc's GFX channel outright (client visibly reacted — shrunken black + transparent — then zero FrameAcks/QoE for the rest of the session). That's the **third** independent confirmation that ANY DeleteSurface aimed at a blank mstsc is fatal — do not use upstream's resize flow on a blank client. QoE-less clients (FreeRDP) never accumulate the evidence window so are never touched. Tunables: `MACRDP_BLANK_RECOVERY_{MIN_QOE,ARM_MS,RETRY_MS,MAX_ATTEMPTS,MAX_CONSECUTIVE_DROPS}`. **LIVE-VERIFIED 2026-07-02: detection is flawless (both blanks caught, zero false positives across ~10 reconnects, rendering sessions disarmed within ~100–230 ms at EDR 6–22 ms) and the REMAP does NOT heal** (mstsc did not re-composite the fresh surface — layer 2 confirmed client-fatal for in-process surface swaps yet again), so recovery relies on the **DROP** → the client reconnects and renders. **UPDATE 2026-07-03 — heal time roughly halved: the default now goes STRAIGHT to the drop, plus a reconnect-storm guard.** Since the remap is verified never to heal mstsc, remap-first only added ~10 s of black (a wasted attempt + a second full detection window) before the drop that actually heals — so `MAX_ATTEMPTS` now defaults to **1** (drop on first detection; ≥2 re-enables remap-first experimentation, e.g. against a non-mstsc QoE client) and `MIN_QOE` tightened **40→24** (~3 s of all-zero evidence — safe because a rendering session disarms on its very first callbacks, and with the ARC cookie a hypothetical false drop is a self-healing reconnect blip, not a lost session). Worst-case blank-to-healed goes ~13–15 s → **~6–8 s** (≈3 s detect + drop + the client's own reconnect loop). New guard: `MAX_CONSECUTIVE_DROPS` (default 3; 0 = uncapped) — each fresh connection re-runs the detector, so a truly-stuck client (every in-process reconnect lands blank) would otherwise loop drop → auto-reconnect → blank → drop *forever*, flashing "Attempting to reconnect" every few seconds; after N consecutive blank-drops with no presenting connection in between, macrdp stops dropping (the detector disarms for that connection, the session stays up) and logs the known-reliable guidance (fully close + reopen the client, or use `--fork-workers`). The counter is process-wide (`Gfx::consecutive_blank_drops`) and resets the moment any connection reports a nonzero decode+render time. **UPDATE 2026-07-05 — the detector is now RTT-AWARE (the ZeroTier false-positive fix).** Live debugging over ZeroTier (~200 ms RTT) proved the EDR==0 signal is UNTRUSTWORTHY on high-latency links: a session that IS visibly rendering reports `timeDiffEDR == 0` on every frame, so the detector force-dropped a WORKING session every ~5 s and the repeated drops poisoned mstsc's surface into a REAL permanent black — the recovery *caused* the blank it exists to fix (the episode initially looked like a regression from the same-day cursor fix #134, which was exonerated three ways: mechanism, a clean FreeRDP render test, and an A/B on the pre-fix build that blanked identically). Fix: the vendored server samples the kernel's smoothed TCP RTT per connection at accept (`TCP_CONNECTION_INFO`, divergence 15) and `h264.rs` gates the detector on it (`blank_rtt_gate`): ≤25 ms behaves exactly as before; up to `MACRDP_BLANK_RECOVERY_MAX_RTT_MS` (default 80) the all-zero evidence window scales linearly with RTT (capped 4×); at/above it the drop lever is WITHHELD for the connection (log marker: `blank recovery DISARMED for this connection — link RTT too high…`) — on such links a false drop cycle is strictly worse than an un-healed blank (recovery there: close+reopen the client). Companion change: `--adaptive-bitrate` seeds a slow link's starting bitrate at ceiling/3 (`MACRDP_ADAPTIVE_SEED_RTT_MS`, default 50) so the first seconds don't overshoot a distant pipe — automating the manual `--bitrate` drop that fixed the ZeroTier freezes live. **LIVE-VERIFIED 2026-07-04 (real mstsc over ZeroTier, twice in one session):** the tuned path fired exactly as designed — `action=Drop attempt=1 max_attempts=1 qoe_reports_all_zero=24` → ARC auto-reconnect → fresh connection rendered, both times, no user action. **Timing nuance from that run:** the "~3 s detect" assumes the LAN QoE cadence (~8 callbacks/s); the cadence scales with *delivered* frame rate, so on a constrained link it stretches (observed ~2/s over ZeroTier → ~9–14 s to the drop) — still ~half the old remap-first path on the same link. The storm guard's *cap* has not fired live (correctly — a presenting connection resets the counter, which the same session demonstrated); its firing path is unit-tested. **To make that drop reconnect *seamless* (not a manual click), macrdp also provisions the Server Auto-Reconnect Cookie** (MS-RDPBCGR ARC; vendored `ironrdp-server` divergence 13, upstreamed 2026-07-02 as PR #1405 — a Save Session Info PDU with `ServerAutoReconnect` sent once after activation; `main.rs` default-on, `MACRDP_AUTO_RECONNECT=0` disables). Without that cookie mstsc treats the drop as a plain disconnect and waits for a manual reconnect (observed); with it, mstsc enters its "Attempting to reconnect" loop and re-establishes on its own (NLA re-auths from cached creds), and the fresh connection renders. Net after the drop: silent indefinite black → auto-detected → one non-destructive remap try → connection dropped → **client auto-reconnects → renders**, no user action. (Combining with `--fork-workers` makes that reconnect land on a fresh *process*, the strongest layer-2 dodge.) **UPDATE 2026-06-23 — `--fork-workers` (opt-in) largely fixes this by ADOPTING xrdp's PROCESS model, not a PDU.** A thin supervisor binds the port and `fork+exec`s a *fresh worker process per connection* (socket handed via `MACRDP_WORKER_FD`); the fresh process dodges layer 2 (mstsc re-maps a fresh surface against a brand-new GFX channel). On real mstsc the always-blank reconnect now renders; a residual ~1/7 blank remains (still client-side — the blank worker's capture/encode/ship logs are byte-identical to a rendering one) and recovers by **reconnecting once more** (no close+reopen needed). Two non-obvious server bugs were fixed to get there: (a) the worker must `std::process::exit` after the connection — a normal return leaves it alive (SCK framework threads block exit), leaking SCStreams until macOS starves capture → a deterministic "blank after 2"; (b) the supervisor serializes (drains the previous worker before spawning the next) to avoid an SCStream-slot overlap race. The teardown DELETE_SURFACE/DYNVC_CLOSE approach above was abandoned (it degrades mstsc); process-freshness is the fix. The supervisor owns the persistent state (virtual display, headless blanking, caffeinate, app-switcher HUD); workers are per-connection and run `shutdown_cleanup` before exiting. **Default OFF, opt-in (`--fork-workers` / `FORK_WORKERS=1`), mirror-primary or `--virtual-display` (headless modes need the supervisor-owned display). Smart-card redirection works under `--fork-workers`** (the `:40242` bridge re-binds per worker since the card lives on the client; verified end-to-end — establish/list/get-status+ATR/connect/transmit — including reconnect, slotd cleanly re-attaching to the fresh worker). FreeRDP/Thincast never needed this. See the `--fork-workers` flag note below and the `project_h264_reconnect_blank` memory. **UPDATE 2026-07-07 — the blank is a TIMING RACE, and `--fork-workers` is a NET NEGATIVE for *interactive* mstsc reconnect (recommend single-process for that use).** A long exhaustive live A/B on real mstsc/WiFi (single-process ×{ARC on/off}, fork-workers ×{ARC on/off}, + a DVC/DRDYNVC/EGFX trace) produced **no code change** — every symptom is mstsc-client-side and macrdp behaves correctly throughout. Findings: **(1) The blank is a race** between mstsc *releasing* its retained surface 0 and the reconnect *re-creating* surface 0. A **fast LAN reconnect beats the release → stale surface → blank**; a **slow reconnect (ZeroTier ~200 ms, OR simply waiting ~15–30 s before reconnecting on WiFi) lets mstsc release first → fresh surface → renders**. This explains the user's "ZeroTier reconnect works but WiFi blanks" paradox — it is **not** UDP (ZeroTier's UDP is at the VPN layer; macrdp's RDP is plain TCP over it, and UDP-multitransport is RTT-gated off ≥80 ms anyway); the latency helps only as *delay*. **(2) `--fork-workers` does NOT heal it** (the retained surface is client-side; a fresh *server* process doesn't reset the *client* surface) **and it ADDS a stall**: mstsc opens an **abandoned extra TCP connection** on every attempt (connects, authenticates, then sends zero RDP bytes → `Broken pipe` at ~6–11 s — confirmed by the DVC trace: no X.224/DVC/EGFX on it), and because the supervisor serializes, that abandoned connection **holds the single worker slot ~7 s**, delaying the real connection → the "connection takes long / times out before the credential dialog" symptom. Single-process has no serialization and connects promptly. **(3) The ">30 s can't connect after X-ing out a blank" is mstsc's own reconnect BACKOFF**, not the server (macrdp accepts a fresh mstsc instantly; a 46 s log gap had zero accepts) and not the ARC cookie (reproduced with `MACRDP_AUTO_RECONNECT=0`). **(4) Blank detection works under both process models** (fires `action=Drop`), but under fork-workers the dominant blank is the Type-A silent/`Broken pipe` connection with no EGFX channel — which the QoE detector cannot catch (no acks to accumulate). **Recommendation: single-process default (drop `--fork-workers`) + `--enable-h264` + blank-recovery + ARC for interactive mstsc-over-WiFi; when a reconnect blanks, close+reopen mstsc (or wait ~15–30 s before reconnecting); or use Thincast/FreeRDP, which reconnect cleanly.** The one server-side idea surfaced (a short RDP-negotiation timeout so an abandoned connection frees the worker slot in ~2 s) only helps fork-workers → not pursued. **STOP re-chasing — the floor was reached again from a new angle.** **UPDATE 2026-07-07 — SOLVED (v0.8.27): a bare core Deactivation–Reactivation HEALS the blank in place, overturning the entire "layer-2 is client-fatal / not server-fixable" conclusion above.** The one lever nobody had cleanly isolated: send an RDP core **Deactivation–Reactivation** (Server Deactivate All → new Demand Active) that does **NOT touch the EGFX pipeline at all** — no DeleteSurface, no DYNVC close, no RESET_GRAPHICS. Every prior "reactivation-ish" attempt bundled a surface delete or a channel close (which *are* client-fatal, as documented exhaustively above); a *bare* core reactivation is uniquely safe. Implementation is a **zero-vendored-change** injection: on a detected blank, `h264.rs` stashes the current desktop size in a shared atomic (`Gfx::perform_blank_reactivate` → `reactivate_request`); `capture.rs::next_update` drains it and emits a **no-op `DisplayUpdate::Resize(current_size)`**, which the vendored server already turns into `deactivate_all` + `Acceptor::new_deactivation_reactivation` — and that call **preserves the static channels**, so the EGFX DVC and our `ConnectionContext`/surface survive (`build_server_with_handle` isn't re-run, `setup_locked` skips because `surface_id` is already `Some`, so **no `resize_with_monitors`/DeleteSurface fires**). A forced IDR follows (`ctx.need_keyframe`). Re-running the core capability handshake makes mstsc **re-map its retained surface 0 and present again** — in place, **no disconnect**, ~1-2 s. **LIVE-VERIFIED on real mstsc/WiFi: 5/5 then 4/4 blanks healed** (EDR=0 → nonzero `time_diff_dr`, `frame_id` mid-stream = same connection, **zero drops** across both runs). This is now the **DEFAULT** first recovery action (`BlankAction::Reactivate`, `MACRDP_BLANK_RECOVERY_REACTIVATE=1`); the old **drop** is now only the fallback if the reactivation ever fails to heal (`max_attempts` forced ≥2). **Detection was also sped up** to ~4 s: on a *static* blank the QoE reports trickle at ~0.3/s so the count path (`MIN_QOE=24`) took ~70 s — a **wall-clock fast-path** (`should_blank_recover`: `since_connect >= MACRDP_BLANK_RECOVERY_MAX_WAIT_MS` (4000) with `>= MACRDP_BLANK_RECOVERY_MIN_WALL_REPORTS` (1) all-zero reports) fires as soon as the arm/retry floors (3 s/4 s) clear. The `>=1` report floor is safe because `arm_delay` (3 s) guarantees a *rendering* session has already presented + disarmed (`qoe_render_seen`) by then, and a false fire is now **cheap** — a brief re-handshake, not a dropped session (which is exactly why we can afford to be aggressive). Still RTT-gated (`blank_rtt_gate`) and QoE-gated, so FreeRDP (no QoE → never accumulates) and high-RTT links (EDR untrustworthy) are untouched. **The reliable recovery is no longer "close+reopen mstsc"** — the server self-heals. `--fork-workers` is no longer needed for this (and remains a net-negative for interactive mstsc, per the prior update). Do NOT reintroduce any surface DeleteSurface/DYNVC-close into the recovery path — the bare reactivation is the whole trick.
- **mstsc holds a ~2-frame AVC420 presentation buffer; the last change before a pause needs trailing "flush" frames (`--flush-frames`, in `capture.rs`).** mstsc only *presents* an H.264 frame once ~2 more frames arrive behind it, so on a static screen the last change (the final keystroke before you pause, a window-to-front) strands in that buffer until something else moves or the periodic keyframe lands — the "typing follows the keyframe" lag. The trap: **ScreenCaptureKit stops delivering frames when nothing on screen changes** (idle samples have no content and are skipped at `capture.rs`), so there is no continuous stream to push the buffer through — raising `--fps` alone does *not* fix it (it only shrinks the per-frame interval). The fix is a **bounded flush-burst**: after each EGFX-accepted frame, `next_update` stashes the BGRA (reused buffer) and arms a counter; its SCK stream-wait is wrapped in `tokio::time::timeout(frame_interval, …)`, so when SCK goes idle the timeout fires and we re-submit the stored frame as a cheap skip-P-frame, `--flush-frames` times (default 4; mstsc needs ≥2; **0 disables**). That drains the buffer within ~2 frame intervals (~33 ms at 60fps), then the stream goes quiet — no always-on cost, and the legacy bitmap path is untouched (the counter only arms on the EGFX path). **Do NOT "fix" this by reinstating idle-frame suppression or relying on a continuous always-on stream** — SCK won't feed one, and suppression strands the trailing frame again (tried and reverted). FreeRDP/Thincast present each frame immediately and need none of this. Verified on M1; periodic `--keyframe-interval` default is 2 s (it self-heals garbled frames but, with the flush-burst, no longer governs typing latency).
- **Resizing/moving the mstsc window drifts audio late — H.264-only (`--enable-h264`).** With H.264, EGFX video frames and the RDPSND audio waves share the *same socket writer* — a single `SharedWriter` (`Rc<Mutex<..>>` over the one TCP connection) cloned per task. Audio rides its own dedicated `mpsc` channel + `dispatch_audio` task (it was carved out of the unified `ServerEvent` channel), but every `audio_writer.write_all` still has to acquire that one shared socket lock — the same lock the EGFX path holds while writing a frame. Legacy bitmaps don't contend the same way (dirty-rect, intermittent, default 15 fps, coalesced through `dispatch_display`), which is why the legacy path is immune. When mstsc freezes the socket during a window resize/move/fullscreen-toggle, `write_all` blocks on the large queued video frames; wall-clock keeps advancing while no audio ships, so the cross-batch audio-lag model in `vendor/ironrdp-server` (`audio_shipped_ms` vs `audio_clock_start`) sees its buffer projection go *negative*, reads it as "client starving," and ships the entire stale backlog late — bloating the client's playback buffer. Each stall adds more negative credit, so without intervention the wave-drop logic stops firing and audio gets *progressively* more delayed the more you resize. Fix (in `dispatch_audio`'s task-local lag loop): when `real_elapsed - audio_shipped > 300 ms` the writer clearly stalled and the queued waves are stale, so resync `audio_shipped_ms` to live; the existing drop-oldest cap then trims the backlog to one `MAX_LAG_MS` (200 ms) of the freshest waves. For a live stream, dropping stale audio is always right — never replay it late. EGFX frames themselves are *not* dropped in dispatch (H.264 inter-frame coding forbids it; they're bounded capture-side by frames-in-flight). FreeRDP/Thincast don't freeze the socket on resize and never showed this. See the `vendor/ironrdp-server` note for the divergence layering (this resync is local-only, distinct from the merged keep-newest PR #1276).
- **Honoring `SuppressOutput` is required to avoid input-lockout on minimize→refocus — but three arming gates are also required, or mstsc freezes on connect / reconnect / stutters under heavy local CPU/IO.** Reported + fixed 2026-05-26 (v0.5.47, debounce + mute-on-minimize landed in v0.5.48). Original bug: minimize mstsc for several minutes with H.264 video flowing → refocus → desktop frozen, audio skips, video speeds up to catch up. Root cause: vendor server was logging `SuppressOutput` as `Unexpected share data pdu` and ignoring it; we kept streaming 60 fps of EGFX frames into a minimized client, which accumulated them, and on refocus mstsc had to chew through the backlog before it could present the current frame — its input dispatch was busy the whole time, ~several seconds of total UI lockout. Fix: vendor server pattern-matches `SuppressOutput` / `RefreshRectangle` and toggles a shared `Arc<AtomicBool> display_suppressed` (exposed via `set_display_suppressed_handle`); `capture.rs::next_update` checks the flag and short-circuits SCK pull + encode + ship while it's set; on the true→false edge a one-shot IDR keyframe is forced so the client resumes from a clean reference frame. **Three arming gates are non-obvious and load-bearing:** (1) **first-EGFX-frame gate** (`first_egfx_frame_sent` in `ScreenCaptureUpdates`) — mstsc's normal connect handshake includes a `SuppressOutput { None }` *before* its display surface is fully initialized; honoring it then leaves mstsc with a half-init'd surface that doesn't recover on un-suppress and the desktop never paints. FreeRDP doesn't issue suppress during connect and was never affected, which is why blanket honor "worked on FreeRDP." Gate the gate so it's only respected after the first EGFX-accepted frame ships. (2) **per-connection reset of the flag** (in `ScreenCaptureUpdates::start`) — the `Arc<AtomicBool>` is owned by the server and survives across disconnect/reconnect; if the previous session ended with it `true`, the *next* connection arms its first-frame gate, immediately reads the stale `true`, and freezes the new session. Reset to `false` at the top of every `start()` call. (3) **per-reader debounce (`SUPPRESS_DEBOUNCE = 1 s`, identical in `capture.rs` and `audio.rs`)** — under heavy local CPU/IO (cargo build, big disk I/O) mstsc emits transient `SuppressOutput { None }` → `RefreshRectangle` pairs tens of ms apart to throttle the server when its receive queue grows. Reacting to those rapid flaps thrashes both the video gate and the audio mute, producing audible stutter and visible video hiccups even though no real minimize occurred. Each reader maintains a local `Option<Instant>` "first observed suppress=true" timestamp and only engages the gate once `elapsed >= SUPPRESS_DEBOUNCE`. Real minimizes (seconds-to-minutes) cross that threshold easily; backpressure flaps don't. Don't remove any of the three gates without re-testing first-connect, reconnect-after-disconnect, long-minimize, AND cargo-build-while-watching-YouTube on mstsc. Mute-on-minimize is default-on (`--no-mute-on-minimize` to disable) and uses the same shared flag + same debounce so audio and video stay coherent through transients.
- **EGFX-over-UDP (`--udp-migrate-egfx`) + minimize/restore froze the picture until reconnect — fixed by proactively de-migrating to TCP on the un-suppress edge.** Reported + fixed 2026-06-29 (PR #99). Symptom on real mstsc/WiFi: with EGFX migrated onto the reliable UDP tunnel, minimizing the mstsc window then restoring it left the **video frozen** (audio, on TCP, resumed fine) until a full disconnect + reconnect to a *fresh* mstsc; an EGFX-on-TCP-from-start session (`UDP_MIGRATE_EGFX=0`) handled the same minimize/restore cleanly. Root cause: once the EGFX DVC has been **Soft-Sync-migrated onto the UDP tunnel**, mstsc's surface doesn't survive the SuppressOutput→restore cycle, and the wedge **watchdog** (above) only de-migrates *reactively* — 3–6 s after restore (confirmed in the log, `since_ack_ms=6641`), *after* we've already shipped the restore frames into the now-stale post-minimize tunnel, by which point switching the transport is too late to heal the client surface. Fix: on the un-suppress (restore) edge, if EGFX is currently on the reliable UDP tunnel, **proactively de-migrate to TCP BEFORE shipping a single frame back into the tunnel** (`Gfx::demigrate_on_resume`, called from `capture.rs`'s existing `was_suppressed` resume edge), so the forced restore-IDR lands over TCP — which mstsc renders across minimize exactly like an EGFX-on-TCP-from-start session. It mirrors the watchdog's de-migrate steps (force IDR + reset the lag baseline + set the `demigrated` latch / `demigrate_request`), is **one-way per connection** (a reconnect starts fresh on UDP), and is a **no-op on the TCP/default and lossy paths** (gated `egfx_on_udp && !egfx_on_lossy` → those paths byte-unchanged). **Verified live on real mstsc** (`--udp-migrate-egfx --bitrate 6 --adaptive-bitrate`): minimize→restore → video comes back; log shows `client resumed from minimize while EGFX was on the reliable UDP tunnel — proactively de-migrating to TCP`. This confirms the freeze was the *transport*, not the unfixable mstsc reconnect-blank surface bug. (Distinct from the SuppressOutput arming-gates note above, which is the always-on minimize handling; this is the UDP-specific layer on top.)
- **Minimizing mstsc for minutes then refocusing → audio plays back 1s+ late, and we can't see it from the server side.** Reported 2026-05-26. When mstsc is minimized while RDPSND audio keeps flowing (e.g. YouTube playing on the Mac while the user does other Windows work), Windows' audio mixer (`audiodg.exe`, a separate process) appears to throttle playback while continuing to drain mstsc's network buffer; on refocus, the buffered backlog plays out — audio lags by however much accumulated. **`WaveConfirm` is not a usable signal for this** — investigated 2026-05-26 by surfacing every confirm via `RUST_LOG=ironrdp_rdpsnd=debug`. Confirms flow continuously through minimize at full ~46/sec rate (one per ~21 ms wave), with no gaps or slowdowns. That means mstsc emits `WaveConfirm` on *arrival/queue* into its dispatch (it has received and forwarded the wave to audiodg), not on *playback completion through speakers*. The actual buffering happens downstream in audiodg, which the RDP stack does not observe. A server-side wave-confirm tracker (the obvious "detect client playback lag" design) would see all confirms arriving fine and never trip its drop logic; it cannot fix this. **Do NOT try to fix this with a `WaveConfirm`-based playback tracker** — verified dead-end. The two realistic options if this becomes worth fixing: (a) heuristically throttle wave emission while mstsc has sent `SuppressOutput` (correlates with minimize but not 1:1 — see the `Unexpected share data pdu` warnings in logs); (b) accept as a Windows audio-mixer behavior we cannot see from RDP and document the limitation (current choice).
- **Cursor sprite shape is process-global on macOS, not per-display.** `NSCursor::currentSystemCursor()` reflects whatever the foreground application set in *this process*'s perspective, not "the cursor on display X." When `--virtual-display` is active, the RDP client sees the cursor shape that whatever app is foreground locally has set (typically the arrow), regardless of what an app on the virtual display might want. Probably fine in practice — the cursor *position* on the virtual display is correct because `input.rs` translates RDP coords by the vdisplay's origin in global space; only the visual shape is shared. Fixing this properly needs a per-display NSCursor query, which AppKit doesn't expose.
- **Oversized cursor sprites (macOS "shake mouse pointer to locate") used to kill the whole session — now downscaled to fit the pointer PDU.** Found 2026-07-04 (killed 6 straight ZeroTier sessions at ~45 s each while live-verifying #133; also present in that morning's logs on the previous build). `TS_COLORPOINTERATTRIBUTE` carries the 32-bpp cursor bitmap as its XOR mask with a **u16 byte length**, so `w*h*4` must stay ≤ 65535 (area ≤ 16383 px). Shake-to-locate enlarges the pointer several-fold and SkyLight returns it at **Retina backing pixels**, so even 128×128 (= 65536 bytes) overflows; ironrdp's `ColorPointerAttribute::encode` correctly rejects it, but the error propagates out of the encode path and **tears down the client loop** — the user sees their session die every time they shake the mouse (jerky remote mouse movement over a laggy link can trigger shake detection too). Fix in `src/cursor/mod.rs::clamp_pointer_size` (called from `poll_shape` after the `--cursor-scale` resize): sprites over the area limit are shrunk aspect-preserving via the existing area-averaged resampler, hotspot tracking exactly; normal-sized cursors (≤ ~127×127) pass through byte-identical. This also protects `--cursor-scale` users (2.0 × a 64 px Retina cursor = 128 px = the same overflow). Log marker at `debug`: `cursor sprite too large for the pointer PDU — downscaled to fit`.
- **`--virtual-display` on mstsc: large legacy frames are strip-split, a release build matters, and the cursor is warped home on disconnect.** Verified 2026-06-02 against remote mstsc. One gotcha plus two fixes: (1) **Legacy big-frame blank on mstsc — FIXED by strip-splitting.** A virtual display is captured 1:1, so `force_full_frame` is *false* — the full WxH paint is sent once on the first frame, then dirty-rects. The bitmap encoder (`vendor/ironrdp-server/src/encoder/bitmap.rs`) packs that whole rect into ONE update PDU (one RLE rectangle per ~`65535/(w*4)` rows), and mstsc has a per-update size/rect-count limit: it renders a single ~1280×720 (≈0.9 MP, ~60 rects) update but DROPS a 1920×1080 (≈2.1 MP, ~135 rects) one, so the desktop never painted (only the ticking-clock dirty-rect rendered → "almost blank, only the menu-bar clock visible"). FreeRDP (tolerant) rendered either. Disambiguated by testing: a 1280×720 vdisplay renders on mstsc, 1920×1080 doesn't — i.e. it's the *size*, not once-vs-repeated. Fix: `capture.rs::split_strips` splits any emitted rect into horizontal strips ≤ `MAX_BITMAP_UPDATE_PIXELS` (= 1280×720, the proven-good size) so each becomes its own smaller, renderable update. Verified: 1920×1080 legacy vdisplay now renders the full desktop on mstsc. (`--enable-h264` is still preferable for bandwidth/quality, but legacy no longer blanks.) (2) **Debug builds make mstsc reset.** `cargo run` (unoptimized) encodes ~400 ms/frame; that stalls the socket and mstsc resets the connection right after the first EGFX frame ("Connection reset by peer"). A release build (`cargo build --release`, ~20× faster encode) holds the connection and renders cleanly. Always test mstsc on a release build. (3) **Cursor-stranding fix.** During a vdisplay session `input.rs::move_to` posts the cursor into the virtual display's slot of the global coordinate space (off every physical panel). The vdisplay outlives the per-connection capture object, so on disconnect the local cursor was stranded and unusable until macrdp exited. Fixed: `ScreenCaptureUpdates` carries a `warp_cursor_home` flag (set when `--virtual-display`) and on `Drop` (per-connection, fires on disconnect) `CGDisplay::warp_mouse_cursor_position`es the cursor to the primary physical display's center. Primary-capture path is unaffected. Signal-exit needs no warp (the OS reaps the vdisplay → cursor reparks).
- **mstsc gates Win-combos by full-screen by default.** mstsc → Local Resources → Keyboard → "Apply Windows key combinations" defaults to "Only when using the full screen", so a windowed mstsc session eats `Win+Tab` (the Cmd+Tab we want to forward) locally as Task View — macrdp never sees the press. The #1 false-positive when debugging "Cmd+Tab doesn't work" is the user being windowed. Set it to "On the remote computer" or go full-screen. `xfreerdp` is more permissive by default and a useful cross-check.
- **Symbolic-hotkey dispatcher needs kernel HID; CGEventPost can't wake it.** WindowServer's internal Cmd+Tab / Cmd+Space / Cmd+Shift+3/4/5 / Mission Control dispatcher only fires on kernel-injected HID events. User-space CGEventPost cannot trigger it regardless of source state or tap location. A `src/virtual_hid.rs` module that registered a virtual USB-style HID keyboard via `IOHIDUserDeviceCreate` was tried for this and reverted — didn't unblock the dispatcher (likely needs entitlements / signing we don't have) and added a parallel scancode-mapping table to maintain. The current design instead reimplements each symbolic combo in user space (`cycle_apps` via AX, `invoke_spotlight` via osascript, `screencapture` via the binary). Do not resurrect IOHIDUserDevice without new evidence.
- **`CAN_LOCK_CLIPDATA` is required for reliable Windows→Mac file paste.** Without it advertised in `MacCliprdrBackend::client_capabilities`, the upstream cliprdr crate's `send_lock` short-circuits (returns None) and Windows Explorer is never told when we're done with a `FileGroupDescriptorW` it gave us. Two visible symptoms on mstsc, both caused by the same missing protocol contract: (1) a rapid follow-up Ctrl-C in Windows after a successful paste is silently dropped — no new FormatList reaches the Mac, so the pasteboard keeps the previous file's URL and Cmd-V re-pastes the prior file. (2) Large downloads (multi-hundred MB) get `CB_RESPONSE_FAIL` partway because the source app decides the descriptor isn't being held and releases it mid-stream. Adding `CAN_LOCK_CLIPDATA` lets cliprdr auto-issue LockData on incoming file-list FormatList and UnlockData on supersession/timeout — verified to fix both symptoms. Cap is essentially free; mstsc/freerdp both negotiate it. *Do not* remove the cap without a compelling reason.
- **Lazy paste (default; `--no-lazy-paste` to opt out) runs the byte fetch on the user's Cmd-V; the fetch competes with the RDP session for the same socket.** The lazy path's `relinquishPresentedItemToReader:` callback runs during interactive use (the user is staring at the screen, expecting responsiveness), and the `FileContentsRequest` chunks ride the same TCP connection as EGFX video + input. We cap parallelism at `LAZY_PARALLEL_CHUNKS = 2` (vs eager's 8) so the inbound socket pressure stays low and mouse/keyboard remain usable during a multi-hundred-MB paste. Raising it would speed downloads but visibly stutter input — verified on real mstsc. The trailing edge of a large paste can also stutter briefly while Finder finalizes (APFS sparse-block materialization + Finder post-validation); not currently mitigated and probably not worth chasing.
- **Windows shell extensions can silently block specific files from CLIPRDR.** When a file has an extension owned by a shell extension (commonly archive tools — 7-Zip / WinRAR / built-in Compressed Folders for `.zip`, `.gz`, `.bz`, `.bz2`, `.7z`, `.rar`, `.tar`), the extension can intercept Explorer's clipboard handling so the FormatList that mstsc forwards either has no `FileGroupDescriptorW` at all (the more common case — `cliprdr=debug` shows `Expiring active locks due to clipboard change (new FormatList received)` with no following `Received FileGroupDescriptorW` line) or never gets sent. To mitigate the surprising-paste UX (Mac pasteboard would otherwise retain the previously-pasted Windows file's URL and Cmd-V in Finder would silently paste the wrong file), `MacCliprdrBackend::on_outgoing_locks_expired` clears NSPasteboard whenever Windows transitions clipboard state without sending a file descriptor — Cmd-V then beeps clearly. Renaming the file to a neutral extension (`.bin`) bypasses the shell handler and copies fine. Not fixable server-side beyond the auto-clear.
- **AAC audio (`--enable-aac`) is raw access units with `cbSize=0`, and the AudioToolbox pull loop has an end-of-stream trap.** Four hard-won facts behind `src/aac.rs`: (1) **Wire format.** The `WAVE_FORMAT_AAC_MS` (0xA106) payload over RDPSND is **raw AAC-LC access units** — object type 2, 1024 frames/AU, NO ADTS/LATM headers and NO in-band AudioSpecificConfig. The AAC `AUDIO_FORMAT` is advertised with `cbSize=0` (no HEAACWAVEINFO blob); the decoder derives its config from the WAVEFORMATEX rate/channels. Verified against FreeRDP's server (`server/common/server.c` advertises 0xA106 with `cbSize=0`; `dsp_fdk_impl.c` configures fdk-aac `AOT=2`, `TRANSMUX=0`). AudioToolbox's `kAudioFormatMPEG4AAC` encoder emits exactly this, so the magic cookie / ASC it can produce is intentionally never sent. (2) **The EOS trap.** `AudioConverterFillComplexBuffer`'s input proc returning `noErr` with zero packets is read by AudioConverter as *end-of-stream* — after the first batch the converter refuses all further input and audio dies after ~3 packets. The proc must instead return a **non-zero sentinel status** (`INPUT_EXHAUSTED`) to mean "no more data this batch"; `encode` treats that as a clean break, leaving the converter's internal <1024-frame remainder buffered for the next call. (3) **The vendored audio-lag model is byte-based and had to learn duration.** `vendor/ironrdp-server`'s `dispatch_audio` task converts each wave's *byte length* to ms via `BYTES_PER_MS = 176.4` (PCM-specific). A ~120-byte AAC AU is ~23 ms, so that math reads the projected client buffer as near-empty and silently disables the drop-oldest/resync lag control. Fix: `AudioWave` gained a third field `Option<f64> duration_ms`; AAC passes `Some(1024/44100*1000 ≈ 23.22)`, PCM passes `None` (unchanged). See vendor divergence (8). (4) **~40–50 ms priming latency** is why it's opt-in — PCM stays the LAN default. Clients without an AAC rdpsnd decode path fall back to PCM automatically because we advertise both, AAC first, and `start()` picks the first mutually-supported format. (MS-RDPEA notes AAC implies unreliable-UDP DVC transport, but RDPSND over the static TCP channel carries AAC fine — FreeRDP's server does the same.) **Client-support matrix (verified 2026-05-30).** Whether a FreeRDP-family client decodes AAC is build-dependent, and it's NOT just the `WITH_FDK_AAC`/`WITH_FAAD2` flags: `freerdp_dsp_supports_format` first delegates to the **FFmpeg DSP backend** when `WITH_DSP_FFMPEG=ON`, and there AAC is the *only* compressed RDPSND codec NOT filtered out (`ffmpeg_codec_is_filtered` hard-drops MP3/GSM/ADPCM/G.711 unless `WITH_DSP_EXPERIMENTAL`, but returns false for `AV_CODEC_ID_AAC`). So a stock Homebrew `sdl-freerdp` 3.26.0 — `WITH_FDK_AAC=OFF WITH_FAAD2=OFF` but `WITH_DSP_FFMPEG=ON WITH_FFMPEG=ON` — **advertises AAC and decodes our raw AUs cleanly via FFmpeg** (live test: server `use_aac=true client_formats=2`; client log `Opening device with format WAVE_FORMAT_AAC_MS [backend WAVE_FORMAT_PCM]` then `rdpsnd_treat_wave` on every ~280-byte AU with zero `aacDecoder_DecodeFrame failed`). The feared raw-AU "needs ASC as extradata" wrinkle did not materialize — FFmpeg's AAC decoder handled it. By contrast the **Thincast** build tested advertised only PCM (`client_formats=1`) — its rdpsnd path has no AAC decoder — so it (correctly) falls back to PCM; that's a Thincast build limitation, not a server bug. mstsc decodes AAC natively. Practical upshot: AAC is effectively the only non-PCM RDPSND codec any modern FreeRDP build will take, and we already advertise it; there is no other compressed format worth adding for the fallback clients. (Test note: on macOS use `sdl-freerdp`/`wlfreerdp`, NOT `xfreerdp` — the latter is the X11 client and fails `pre_connect` with no `$DISPLAY`.)
- **RDPSND `wFormatNo` indexes the CLIENT's format list, not the server's — latent bug exposed by AAC.** `start()` returns the `wFormatNo` stamped onto every Wave2 PDU, and the client resolves the wave's codec as `ClientFormats[wFormatNo]` — an index into the *Client Audio Formats PDU the client sent back*, which is its supported subset of our server list, compacted in server order. FreeRDP/Thincast hard-reject `wFormatNo >= NumberOfClientFormats` (`rdpsnd_recv_wave2_pdu` / `rdpsnd_recv_wave_info_pdu`) and drop the wave silently — no audio. For years `audio.rs::start()` returned the *server*-list index and it worked only because we advertised a single format (server idx == client idx == 0). The moment `--enable-aac` made the list `[aac, pcm]`, a PCM-only client (Thincast advertised `client_formats=1`) got `wFormatNo=1` for PCM's *server* slot while its own list had PCM at index 0 → `1 >= 1` → every wave rejected → silent. mstsc happened to keep working because it lists AAC at index 0 in both. Fix: choose the format by server preference (AAC-first, so we know what to encode), then return that chosen format's position in `client_format.formats` (the client list). **Do NOT revert to the server-list index** — the old code comment claiming "wFormatNo indexes the server list, client-list index causes wrong pitch" was a misdiagnosis: wrong pitch only happens if you pick the format by scanning the *client* list first and matching loosely (you can select a different rate than you encode). Picking via server preference and *then* translating that exact format to its client-list slot is correct for both mstsc and FreeRDP, because the index always points at the format actually being sent. Verified: Thincast/FreeRDP `use_aac=false` PCM fallback was silent before, plays after.
- **HiDPI / Retina capture (`--hidpi`) — what's load-bearing and what's client-bound.** Established 2026-05-30 building the feature. (1) **SCK delivers backing pixels cleanly.** Setting `SCStreamConfiguration` width/height to the display's backing size (`CGDisplayMode::pixel_width/height`, e.g. 3024×1964) makes SCK deliver a full crisp backing-resolution buffer with the existing `scales_to_fit(true)`/`preserves_aspect_ratio(false)` config — `pb_width/pb_height` come back at 3024×1964, not downscaled. A prior (2026-05-17) "blank screen" attempt did NOT reproduce; current screencapturekit 2.1 handles it. Use `display_mode().pixel_width/height` for backing, NOT `CGDisplay::pixels_wide/high` (canonical mode = points on many configs). (2) **Input is resolution-agnostic for free** — `input.rs::move_to` scales RDP coords against `CGDisplay::bounds()` *points*, not the framebuffer, so doubling the framebuffer self-corrects; no input change needed. (3) **`force_full_frame` must treat backing as native** — it compares the configured size against `display.width()` (SCK points); a backing-size capture (3024 ≠ 1512) would wrongly force full frames + warn. Fixed to accept EITHER the point size or the backing size as a 1:1 native capture (dirty-rects stay valid; the EGFX/H.264 path submits full frames regardless so it's unaffected either way). (4) **Do NOT scale the cursor.** SkyLight's `SLSGetGlobalCursorData` already returns the pointer at backing/physical pixels (≈28 px on Retina) and RDP clients render pointer bitmaps at 1:1 device pixels regardless of session resolution — so the same bitmap is correct at points OR backing. An attempt to upscale it by the backing/points ratio made the pointer 2× too big; reverted. An *opt-in* `--cursor-scale` multiplier (default `1.0` = native size, no resize) was later added for comfort: some clients draw the pointer at 1:1 device pixels while upscaling the desktop image to their window, which makes the native-size pointer look small, so bumping it (e.g. `2.0`) enlarges the bitmap **and** its hotspot together via an alpha-weighted area resample (`cursor/mod.rs::scale_cursor`) — the hotspot stays exact at any value. An auto-scale-to-points *default* was tried during this work and rejected: the user preferred native size, and a separately-reported Retina "cursor shrank / hotspot off" turned out to be a **stale mstsc pointer cache** (cleared by reconnect / close+reopen), not a server scaling bug — so the default still does NOT scale. (5) **The lag is the client, not the server.** VideoToolbox encodes 3024×1964 in ~10 ms/frame (99 fps of headroom, under the 16.67 ms/60fps budget) — the server keeps up and doesn't drop frames. But **mstsc decodes 4× the pixels per frame and, with its documented 2-frame presentation buffer (now of 4×-bigger frames), feels laggy at HiDPI**; Thincast/FreeRDP decoders stay snappy. This is a client characteristic, documented not fixed — which is why `--hidpi` is opt-in and best paired with a capable client. **Pooling the per-frame `CVPixelBufferCreate` was tried and is NOT worth it (2026-06-21).** The suspected lever — the per-frame ~9 MB NV12 `CVPixelBufferCreate` in `videotoolbox.rs::encode_frame` — was replaced with a per-encoder `CVPixelBufferPool` (IOSurface-backed, via `IOUSBHostControllerInterface`'s sibling `CVPixelBufferPoolCreate`/`…CreatePixelBuffer`) and benchmarked A/B against the per-frame path (an `MACRDP_H264_NO_POOL` toggle gated the fallback). Result: **~parity** — submit cost 4.0/4.1 ms (1080p) and 11.2/11.1 ms (HiDPI), throughput 233/231 fps (1080p) and 93/91 fps (HiDPI), i.e. ≤2% and within noise. Why: the per-frame create is **not** the bottleneck (the allocator + CoreVideo cache recycle the freed buffer warm), and the path is **fill-bound** — dominated by the vImage BGRA→NV12 conversion (~11 ms ⇒ ~90 fps at HiDPI), which pooling doesn't touch. The change was **reverted** (correct but no measured gain; project favors minimal changes). **Do NOT re-attempt CVPixelBuffer pooling for speed** — the encode path's real ceiling is the vImage fill + the HW encoder, not pixel-buffer allocation. (And neither moves the client-side decode floor.)
- **Smart-card redirection is a user-space PC/SC IFD handler, NOT a kext — and that's the right layer, not a compromise.** The recurring "why not a kernel driver?" question. The decision falls out of *what layer the redirection lives at*: MS-RDPESC carries **PC/SC calls** (`SCardConnect`/`SCardTransmit`/APDUs), not raw USB packets, so the macOS side just needs a *virtual PC/SC reader whose backend is the network* — which is exactly what an **IFD handler** is. There is no physical device to model on the Mac, so there is nothing for kernel code to do. macOS's smart-card stack (`com.apple.ifdreader` / slotd / CryptoTokenKit) is **user-space by design** and loads third-party reader drivers as plain **dylibs** (the IFDHandler v3.0 C ABI) from `/usr/local/libexec/SmartCardServices/drivers/` — Apple's documented, supported plug-in point, needing **no special entitlements** to reach the PC/SC stack. A kext would be the *wrong* layer (kernel code is only needed for **raw-USB passthrough** — modelling the physical reader as a kernel USB/HID device, a different and much lower-level feature; the earlier `jiegec/usbip` raw-USB detour is what confirmed *generic* USB redirection needs kernel-level code — historically a kext, and on modern macOS/Apple Silicon either a **DriverKit System Extension** or a **user-space virtual USB host controller** via `IOUSBHostControllerInterface` driving Apple's built-in `AppleUSBUserHCI` provider ("UserHCI" — no kext, no dext; binary forensics of VirtualHere's client, `nm`/`strings` showing `IOUSBHostControllerInterface`/`IOUSBHostCI*StateMachine`/`AppleUSBUserHCI`/`OSX11ClientDriver.mm`, suggest this is the route it takes — note the device is still registered in the IORegistry by Apple's *own* kernel HCI provider, just driven from user space, so it's "no third-party driver," not "no driver at all"; using it needs the managed entitlement `com.apple.developer.usb.host-controller-interface` — Apple-granted *on request* via Feedback Assistant + Team ID, and crucially NOT the contentious `com.apple.developer.driverkit.transport.usb` that stalls `usbipd-mac`. See **`docs/usb-redirection-feasibility.md`** for the full feasibility writeup if generic USB redirection is ever pursued) — which is precisely why the smart-card "device-class" route was chosen instead to stay protocol-only) **and** an expensive one: kexts are deprecated, and loading one on modern/Apple-Silicon macOS needs kext-signing (a case-by-case Apple grant) or SIP disabled, plus user approval + a **reboot**; the sanctioned successor — **DriverKit / System Extensions** — is no better here, for three reasons: (1) a dext needs **restricted entitlements you can't grant yourself** — the base `com.apple.developer.driverkit` plus a transport family like `com.apple.developer.driverkit.transport.usb`, requested from Apple with a written justification (they can decline) and baked into a **provisioning profile signed against a paid Developer account** — so no ad-hoc signing, no file-copy install; (2) it ships **as a System Extension embedded in a notarized `.app`**, activated via `SystemExtensions.framework` with a System-Settings approval prompt (plus reduced-security friction on Apple Silicon) — a far heavier install/first-run flow than dropping a bundle into the drivers dir; (3) most fundamentally **it's the wrong layer** — DriverKit models *real hardware transports* (USB/PCI/HID/serial/net/block), there is **no CCID / PC-SC / smart-card family** — the USB family (USBDriverKit, on the IOUSBHost classes) is the closest, and that's what generic USB-over-IP uses to present a remote device (generic USB-over-IP DOES synthesize a virtual USB device — whether via a dext, or (as VirtualHere's client appears to, per its symbols) a user-space virtual host controller on `IOUSBHostControllerInterface`/UserHCI; an earlier version of this note wrongly said synthesizing a virtual device wasn't possible, and a later one wrongly pinned VirtualHere on DriverKit). To use it for smart cards you'd drop down to that USB layer and **emulate a physical USB CCID reader at the transport level** — exactly the raw-USB route we avoided, far more code (USB descriptors, CCID class protocol, bulk endpoints) — only to rely on macOS's built-in CCID/PC-SC stack to surface the **same PC/SC reader the IFD handler gives you directly**, one layer up. So DriverKit is pure overhead: permission gymnastics + a hardware-transport abstraction, to reach where the user-space IFD handler already sits with a file copy and no entitlements. Being a small user-space dylib is also what let the handler be written **from scratch (MIT/Apache)** instead of shipping the GPL `vpcd` from vsmartcard, and installs as a normal file copy (one admin prompt) rather than a kernel module. The **one** cost accepted from this route: macOS loads a third-party IFD driver **only on a USB hotplug** matching the bundle's VID/PID, so a headless server needs a dummy USB dongle permanently attached as a trigger — minor next to the signing/SIP/reboot/entitlement tax a kext would impose. (Unrelated: the "no root, no kext, no FUSE" notes elsewhere are about the **drive-redirection NFS mount**, a different feature.)
- **Generic USB redirection (`--enable-usb-redirection`, EXPERIMENTAL) — the hard-won traps.** macrdp presents a client-redirected USB device via the public `IOUSBHost` **UserHCI** virtual host controller (`src/usb_redirect/usb_spike.m` is the quarantined SPI boundary; `src/rdpeusb.rs` divergence 16 is the URBDRC transport). Verified: a redirected flash drive mounts on the Mac. Gotchas that cost real debugging time:
  - **It NO-OPS without the entitled build.** The UserHCI controller needs `com.apple.developer.usb.host-controller-interface`, which only exists in the signed+provisioned bundle. A plain `cargo build` logs `controller unavailable (needs the entitled build)` and presents nothing — so all live testing is on the entitled `.app` (`PROVISION_PROFILE=…macrdpprov2.provisionprofile …make-app.sh`).
  - **You cannot test bulk/mount from a macOS *client* (loopback).** macOS libusb can't kernel-detach a mass-storage device to claim the interface (`LIBUSB_ERROR_ACCESS`), and every bulk transfer needs a claimed interface. So the loopback proves enumeration/encoding but not the mount — you need a **Linux or Windows** client. The proven rig: **UTM-QEMU** Ubuntu + FreeRDP `/usb:...`, with a **USB-2.0 hub** in the path (QEMU passthrough of a USB-3 SuperSpeed device fails `Invalid ep0 maxpacket: 9`; the hub forces high-speed) and a guest `udev MODE=0666` for libusb.
  - **`status=1` in the `[usb2]` log means SUCCESS, not stall.** `IOUSBHostCIMessageStatusSuccess`'s raw enum value is 1; the completion log prints the raw value. A stall is a *different* value. Don't read `status=1` as an error (this genuinely misled a diagnosis).
  - **A device reset destroys+recreates the endpoint at the SAME key** (new Active object, fresh ring) while an in-flight completion still points into the OLD freed ring → SIGSEGV in the completion memcpy. The liveness (`endpointState==Active`) check is insufficient because the *new* endpoint is Active; the fix is **endpoint-OBJECT identity** — the completion captures the endpoint object at raise time and drops itself if `endpoints[key]` was replaced. A mass-storage Bulk-Only Reset (`bmReq=0x21 bReq=0xff`) triggers exactly this.
  - **A client can announce ONE physical drive TWICE** with instance ids differing by a byte (FreeRDP does this — `…d31`/`…d32`). Presenting both spins up two virtual drives that **duel over the single client device** → conflicting SCSI, 10 s timeouts, failed mount. So `drive_device` dedups on the device's **stable hardware identity (`VID:PID:bcdDevice`)**, not the client's per-announce instance id. (Two identical-model drives collapse — true multi-device needs iSerialNumber, deferred.)
  - **Control-OUT is forwarded on the control STATUS stage** (no-data requests are SETUP→STATUS): mass-storage reset / Clear-Feature reach the real device via a generic `CONTROL_TRANSFER_EX`. The standard requests the host controller / SelectConfiguration already own — SET_ADDRESS/CONFIGURATION/INTERFACE — deliberately stay a **local ACK** (forwarding SET_CONFIGURATION would fight our own `select_configuration`). Full running log in `docs/usb-redirection-feasibility.md` + `[[project_usb_redirection_feasibility]]`.
- **The app-switcher HUD (`--app-switcher-hud`) draws a REAL window in a separate helper process — do NOT reinstate the in-frame-composited overlay.** macrdp's Cmd+Tab/Option+Tab cycle had no visible switcher (you cycled "blind"). The HUD adds one, and the load-bearing design choice is *how* it's drawn. macrdp's SCK content filter excludes nothing (`capture.rs` `with_excluding_windows(&[])`), so **any real on-screen window from any process is captured for free** — at **zero per-frame cost** to the capture loop. So the HUD is a real borderless `NSPanel` drawn by a **separate Swift helper** (`macrdphud`, `gui/Sources/macrdphud/`), NOT pixels macrdp paints into each captured frame. The 2026-05-21 attempt did the latter (hand-composited the switcher into every BGRA frame) and was reverted for laggy typing — that per-frame cache/select cost is exactly what the real-window approach avoids. Two constraints forced the separate process: (1) AppKit UI needs the **main thread + a pumped runloop**, and macrdp's main thread is owned by `#[tokio::main]` (no AppKit runloop; `runloop_thread.rs` is not safe for AppKit UI); (2) the panel must be **non-activating** (`.nonactivatingPanel`, `canBecomeKey=false`, `orderFrontRegardless`, `.popUpMenu` level, `.canJoinAllSpaces`) so it floats above the AX-activated app **without stealing keyboard focus**. macrdp pushes SHOW/ADVANCE/HIDE over loopback (`src/switcher_hud.rs`, best-effort `try_send` so input never blocks); the helper resolves app icons **from the bundle on disk** (`NSWorkspace.icon(forFile:)`) **+ caches per pid** — `NSRunningApplication.icon` was flaky on repeated calls (icons randomly went blank across sessions). macrdp auto-spawns the helper (`locate_hud_helper`: `MACRDP_HUD_HELPER` → bundled `Contents/Resources/macrdphud` → dev `gui/.build/release/macrdphud`) and passes its pid so the helper self-exits if macrdp dies. Verified end-to-end on a real client incl. `--virtual-display` (panel centers on the captured display id). **Known limit:** snappy on `--virtual-display` (1:1) but the **mirror-primary / client-resolution-auto-adopt path is less snappy** — that's the general non-native-capture scaling lag (full frames; see the scaling quirk), which the HUD merely rides on, not a HUD bug.
- **The Cmd+Tab/Option+Tab cycle now makes the LANDING app always surface — a deliberate deviation from native macOS, and it affects the switcher with OR without the HUD.** Native macOS Cmd+Tab activates an app's menu bar but does NOT open a window for a **running-but-windowless** app (Notes/Calendar/Mail with their window closed) and does NOT **un-minimize** a minimized-only app — so "switching" to one shows nothing, which reads as a bug for remote use. `ax_make_frontmost` therefore, **on the app you release on** (commit): raises + sets `AXMain=true` on a window (AXMain, not AXRaise alone, is what moves stacking for Electron/Calendar et al.), **un-minimizes** the landed window — with a **deferred re-raise (~420 ms on a detached thread)** because the genie un-minimize animation is async and an immediate raise races it and lands the window *behind* others — **reopens** a windowless app via `/usr/bin/open -b <bundle>` (LaunchServices reopen → creates a window; only for the confirmed-running landing app, layered on AXFrontmost), and **unhides** a hidden (Cmd+H) app. All of this is **gated to the committed/landing app** (`reopen_if_windowless`/`effective_unminimize`), so apps merely *cycled through* during the hold are not un-minimized, reopened, or flickered. `--unminimize-on-switch` still additionally un-minimizes per cycle *step* during the hold. This changes the core switcher's default (not just the HUD) — chosen because for a remote desktop "I switched to it, show it" is the expectation; pass nothing extra to get it.
